# Contributing

Use the Go versions listed in CI, install `pre-commit`, and run `make check`
before opening a pull request. CLI changes should include deterministic tests
and preserve non-interactive operation for automation.
