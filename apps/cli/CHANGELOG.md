# Changelog

Releases use Semantic Versioning and signed `vX.Y.Z` tags. Keep an `Unreleased`
section in review PRs; GoReleaser uses GitHub-native release notes.

## Unreleased

- Initial public CLI hardening baseline.
