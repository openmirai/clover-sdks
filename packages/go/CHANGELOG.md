# Changelog

Releases use Semantic Versioning and signed `vX.Y.Z` tags. Keep an `Unreleased`
section in review PRs; GitHub generates the published release notes.

## Unreleased

- Initial public SDK hardening baseline.
