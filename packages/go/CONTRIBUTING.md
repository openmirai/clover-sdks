# Contributing

Use the Go versions listed in CI, install `pre-commit`, and run `make check`
before opening a pull request. Keep exported API changes documented and add
offline tests for transport behavior. Do not commit generated output, secrets,
or local credentials.
