# Changelog

This package follows Semantic Versioning. Add user-visible changes under
`Unreleased`; release tags promote that section to a version heading.

## Unreleased

- Adopt Clover V2 transport (`/api/v1`, CommonResponse unwrap, ErrorResponse) and send `X-Request-ID`.

- Initial public SDK hardening baseline.
