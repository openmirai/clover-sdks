# Contributing

Install the development extra with `python -m pip install -e '.[dev]'` and
enable hooks with `pre-commit install`. Keep API behavior and compatibility
documented, add regression tests for changes, and do not commit `dist/`,
coverage files, virtual environments, or secrets.
