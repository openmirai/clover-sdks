# Contributing

Use JDK 17 or newer, enable the Maven pre-commit hook, and run `mvn verify`
before opening a pull request. Add JUnit coverage for behavior changes and
keep Checkstyle and SpotBugs clean. Do not commit `target/` or credentials.
