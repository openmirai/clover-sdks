# Contributing

Use Rust 1.82 or newer, enable `pre-commit`, and run the full Cargo check set
before opening a pull request. Add offline tests for behavior changes and do
not commit `target/`, generated packages, or credentials.
